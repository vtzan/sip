# Generated runtime config (build with `make`)
opensips.cfg

# Compose / rotation secrets
.env
tls-rotation/rotation.conf

# Never commit private keys / certificates / ACME state
tls/*.pem
tls/*.crt
tls/*.key
tls/*.p12
tls/*.pfx
**/lego/
**/certificates/
